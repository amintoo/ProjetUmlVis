import txt from './person.txt';

class parser {

  let visibility = new RegExp('%(<\w+>\s+){0,3}');

  let method_name = new RegExp('\w+\s*');

  // TODO
   public Parcourir(Person: file) {
   //comprend comment faire
    let person = [target.file];

    for(let i =0; i< person.length; i++ ) {

      let Vregex = new RegExp('%(<\w+>\s+){0,3}')     //visibility
      let Tregex = new RegExp('%(\w|[<>[\]])+\s+')    //return type
      let Mregex = new RegExp('\w+\s*')               //method name
      let ?regex = new RegExp('\([^\)]*\)')           //method parameters


      // recuperer les attributs (Regexp)
      public verifier() {

      }

    }
  }

}
